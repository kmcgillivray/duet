<div class="row">
  <div class="col-md-8 center-block">
    <div class="card-list">

      <% @advertisements.each do |advertisement| %>
        <div class="card">
          <h2><%= link_to advertisement.title, advertisement %></h2>
        </div>
      <% end %>
    </div>
  </div>
</div>
